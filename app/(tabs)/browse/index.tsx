import React from "react";
import { EmptyScreen } from "@/components/EmptyScreen";

const Browse = () => {
	return (
		<EmptyScreen
			title="No Browse Files"
			description="Browse files will appear here."
			iconName="clock.fill"
		/>
	);
};

export default Browse;
