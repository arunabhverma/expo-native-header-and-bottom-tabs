import React from "react";
import { EmptyScreen } from "@/components/EmptyScreen";

const Shared = () => {
	return (
		<EmptyScreen
			title="No Shared Files"
			description="Shared files will appear here."
			iconName="folder.fill.badge.person.crop"
		/>
	);
};

export default Shared;
